---
indicator: submit
auto_fieldset: 1

elements:

  - type: Text
    name: email
    label: E-Mail
    constraints:
      - Required
      - type: Callback
        message: Such email do not exists in our database.

  - type: Submit
    value: Retrieve Password
    name: submit

constraints:
  - SingleValue